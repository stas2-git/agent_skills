# Future Improvements

Ideas intentionally deferred from the first pass:

- `capture-front-window`
- `capture-window-region` relative to front-window bounds
- interactive region selection
- repeated capture / watch mode
- optional region preview overlay for debugging
- shared state integration with `window-control`
- optional `mss` backend for higher-frequency capture loops
